{ Sample program
  in TINY language -
  computes factorial
}
read x; { input an integer }
if 0 < x then { don't compute if x <= 0 }
  for y := 0;y < 5;y := y + 1 then
    fact := 1;
  end;
  write y  { output factorial of x }
end
